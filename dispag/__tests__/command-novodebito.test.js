const config = require('../command-novodebito/novodebito_handler')

test('adds 1 + 2 to equal 3', async () => {
    expect(1+ 2).toBe(3)
})


test('adds 1 + 2 to equal 3', async () => {
    expect(1+ 2).toBe(3)
})